export const configurations = {
    productTable: process.env.PRODUCT_TABLE,
    productQueueUrl: process.env.PRODUCT_QUEUE_URL,
    sellerId: "sellerId", //@TODO -> seller id should be taken from the token
};
